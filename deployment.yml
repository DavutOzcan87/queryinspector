apiVersion: apps/v1
kind: Deployment
metadata:
  creationTimestamp: null
  labels:
    app: urlinspect
  name: urlinspect
spec:
  replicas: 1
  selector:
    matchLabels:
      app: urlinspect
  strategy: {}
  template:
    metadata:
      creationTimestamp: null
      labels:
        app: urlinspect
    spec:
      containers:
      - image: davutozcan/urlinspector:2
        name: urlinspect
        resources: {}
      imagePullSecrets:
        - name: registry-dwt-development
status: {}
---
apiVersion: v1
kind: Service
metadata:
  creationTimestamp: null
  labels:
    app: urlinspect
  name: urlinspect
spec:
  ports:
  - name: 80-80
    port: 80
    protocol: TCP
    targetPort: 80
  selector:
    app: urlinspect
  type: ClusterIP
status:
  loadBalancer: {}
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: urlinspect
  annotations:
    external-dns.alpha.kubernetes.io/hostname: urlinspector.davutozcan.com
    kubernetes.io/ingress.class: "nginx"
    cert-manager.io/cluster-issuer: "letsencrypt-prod"
    nginx.ingress.kubernetes.io/force-ssl-redirect: "true"
spec:
  tls:
    - hosts:
        - urlinspector.davutozcan.com
      secretName: urlinspect
  rules:
    - host: urlinspector.davutozcan.com
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: urlinspect
                port: 
                  number: 80